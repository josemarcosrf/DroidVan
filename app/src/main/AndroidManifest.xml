<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!--
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    -->

    <!-- Application -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <!-- Navigation Activity which is the main activity for now -->
        <activity
            android:name="project.van.fionaremote.LightSwitchActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        <activity
            android:name="project.van.fionaremote.VoiceLightCtlActivity"
            android:label="@string/title_voice_ctl"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        -->

        <activity
            android:name="project.van.fionaremote.TimerActivity"
            android:label="@string/title_timer_activity"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <!--
        <activity
            android:name="project.van.fionaremote.WebControlPanelActivity"
            android:label="@string/title_web_control_panel"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        -->

    </application>

</manifest>